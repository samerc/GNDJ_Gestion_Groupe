# .NET
bin/
obj/
*.user
*.suo
*.cache
*.vs/
*.DotSettings.user

# Node
client/node_modules/
client/dist/
client/.vite/

# IDE
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store

# Environment
.env
.env.local
.env.*.local
appsettings.Local.json
# Server-only secrets — NEVER commit. Created on the server (see docs/DEPLOYMENT.md Part 4).
appsettings.Production.json

# Docker
pgdata/

# Uploads
uploads/

# Migration source data — contains real member PII, never commit
reinscriptions/
# Application logs (may contain PII / IPs)
logs/
*.log
# scratch
read-xlsx.csx

# Local scratch / data-review files (PII) — never commit
tmp_*

# Per-machine deploy target (remembered by deploy/update.ps1)
deploy/target.txt

# Ops secrets + local runtime state (backup/health scripts) — never commit
deploy/ops-alert.config.json
deploy/rclone.conf
deploy/health-state.txt

# Local build-artifact package (created by deploy/publish.ps1)
/publish/

# ASP.NET DataProtection keys (per-machine, never commit)
dataprotection-keys/
**/dataprotection-keys/
temp/

# Local pen-testing crash course / findings playbook (do not commit — describes live weaknesses)
docs/SECURITY_TESTING.md

# CU guide screenshots + generated PDF contain real member data (names/DOB/medical) —
# keep out of git; blur before distributing.
docs/guides/screenshots/
docs/guides/*.pdf

# One-off go-live data script with real member PII (maîtrise phones/emails) — keep out of git.
deploy/golive/import_maitrise_contacts.sql
