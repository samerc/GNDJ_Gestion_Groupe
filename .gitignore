# .NET
bin/
obj/
*.user
*.suo
*.cache
*.vs/
*.DotSettings.user

# Node
client/node_modules/
client/dist/
client/.vite/

# IDE
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store

# Environment
.env
.env.local
.env.*.local
appsettings.Local.json
# Server-only secrets — NEVER commit. Created on the server (see docs/DEPLOYMENT.md §5).
appsettings.Production.json

# Docker
pgdata/

# Uploads
uploads/

# Migration source data — contains real member PII, never commit
reinscriptions/
# Application logs (may contain PII / IPs)
logs/
*.log
# scratch
read-xlsx.csx

# Local scratch / data-review files (PII) — never commit
tmp_*
